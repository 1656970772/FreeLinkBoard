# M6 大画布渲染 Smoke

- 时间：2026-05-15 23:57:52
- 分支：main
- 基础提交：a1222be feat: add M6 large board performance metrics

## 本次进展

- 继续执行 M6 Task 2：renderer large-board smoke。
- 在 `tests/renderer/board/BoardCanvas.test.tsx` 增加 1000 节点、1600 边的 BoardCanvas smoke。
- 断言首屏 fixture 节点会渲染、远端节点不会挂载、DOM node root 数量低于 150，并确认节点层与边层至少触发 2 次 `SpatialIndex.query()`。
- 补充 canvas mock 的 `quadraticCurveTo`，让测试环境覆盖 rounded elbow 边路径可能调用的真实 Canvas API。

## 多 Agent 结论

- 子代理只读审查确认 selector 方向稳健：使用 `[data-testid^="board-node-fixture-node-"]` 避免误计 `board-node-content-*` 和 resize handle。
- 子代理提示该 smoke 应定位为 DOM virtualization 回归保护，不代表完整 renderer 性能保证；边和节点索引构建成本仍由后续 M6 任务继续覆盖。

## 验证

- `npm.cmd test -- tests/renderer/board/BoardCanvas.test.tsx`：5 tests passed。
- `npm.cmd test`：25 files / 187 tests passed。
- `npm.cmd run typecheck`：passed。
- `npm.cmd run build`：passed。
- `git diff --check`：passed。
