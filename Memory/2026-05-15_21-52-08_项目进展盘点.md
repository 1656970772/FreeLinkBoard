# 项目进展盘点

时间：2026-05-15 21:52:08

用户要求：
- 查看 FreeLinkBoard 项目进展到哪里。

已检查来源：
- 根目录 Git 状态。
- worktree `F:\MyTools\FreeLinkBoard\.worktrees\codex-m0-m1-foundation`。
- 设计规格 `docs/superpowers/specs/2026-05-14-freelinkboard-design.html`。
- M2、M3、M3b、M4 计划与 Memory 记录。
- 当前分支测试、类型检查、构建命令输出。

结论：
- 根目录 `main` 目前主要是文档与 worktree 管理；真正的应用源码在 worktree 分支 `codex/m4-edge-interactions`。
- 项目已从 M0-M1 推进到 M4 蓝图式连线核心交互收尾阶段。
- M0-M1、M2、M3a、M3b 已有开发记录与验证记录。
- M4 已实现 Tab 相连节点、Ctrl+L 连线、连线样式工具条、固定点插入/拖拽，并完成多轮修复。
- 本次新跑 `npm.cmd test`：18 个测试文件、139 个测试通过。
- 本次新跑 `npm.cmd run typecheck`：通过。
- 本次新跑 `npm.cmd run build`：通过。

待处理与风险：
- M4 自动化验证和构建验证已通过，但仍需要真实桌面交互手感复核。
- 重点复核 Tab 连续输入、Ctrl+L 拉线、toolbar 连续调整、fixed point 拖拽。
- M5 搜索、设置、快捷键、关闭前保存检查和体验收口尚未开始。
- M6 性能、稳定性、Electron E2E、打包和发布检查尚未开始。
- 根目录存在未跟踪的 `AGENTS.md`、`Memory/` 和 `NUL.heapsnapshot`；`main` 比 `origin/main` 超前 1 个提交。
