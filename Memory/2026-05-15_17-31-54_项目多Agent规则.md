# 项目多 Agent 规则

时间：2026-05-15 17:31:54

用户要求：

- 将“本项目任何时候，尽可能使用多 agent”写入项目规则。

已执行：

- 在项目根目录创建 `AGENTS.md`。
- 将多 Agent 协作规则写入 `AGENTS.md`。
- 规则包含：可拆分任务优先开启子代理；同一 bug 修过一次仍复现时直接开启子代理；主代理负责整合、验证和中文汇报。

来源：

- 用户在当前对话中的明确指令。
